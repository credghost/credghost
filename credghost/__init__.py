"""CredGhost — NHI and agent credential governance CLI.

Phase 1: read-only AWS scanning. Builds a unified inventory of non-human
identities (service accounts, API keys, IAM roles, access keys), scores their
risk, and produces terminal / JSON / HTML reports.
"""

__version__ = "0.1.0"
